# Reflection-Detector
Burp Suite extension for automated reflection detection across parameters with context-aware analysis (HTML, Attribute, Script, JSON). Designed for efficient XSS triage with filtering, search, and multi-threaded scanning.
